services:
  datagen:
    build:
      context: ..
      dockerfile: datagen/Dockerfile
      args:
        PYTHON_IMAGE: ${PYTHON_IMAGE:-python:3.13-slim}
    image: nanochat-datagen:latest
    container_name: nanochat-datagen
    environment:
      # OpenRouter credentials and metadata
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - OPENROUTER_MODELS=${OPENROUTER_MODELS:-}
      - OPENROUTER_REFERRER=${OPENROUTER_REFERRER:-BRADDLEZ}
      - OPENROUTER_TITLE=${OPENROUTER_TITLE:-OOGA}
      # Dataset targets
      - SFT_TARGET=${SFT_TARGET:-0}
      - DPO_TARGET=${DPO_TARGET:-0}
      - DATAGEN_POLL_SECS=${DATAGEN_POLL_SECS:-15}
      # Automatic increments per poll when no remaining target work
      - SFT_INCREMENT=${SFT_INCREMENT:-2}
      - DPO_INCREMENT=${DPO_INCREMENT:-2}
      # Base dir for datasets (mounted volume)
      - NANOCHAT_BASE_DIR=/data
      # Server options
      - HOST=0.0.0.0
      - PORT=8981
      # Parallel workers
      - WORKERS=${WORKERS:-5}
      - REDIS_URL=${REDIS_URL}
      - USE_CELERY=${USE_CELERY:-true}
    # For local testing, uncomment to expose the service
    # ports:
    #   - "8981:8981"
    volumes:
      - datagen_data:/data
    restart: unless-stopped

  celery-worker:
    build:
      context: ..
      dockerfile: datagen/Dockerfile
      args:
        PYTHON_IMAGE: ${PYTHON_IMAGE:-python:3.13-slim}
    image: nanochat-datagen:latest
    container_name: nanochat-datagen-worker
    depends_on:
      - datagen
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - OPENROUTER_MODELS=${OPENROUTER_MODELS:-}
      - OPENROUTER_REFERRER=${OPENROUTER_REFERRER:-BRADDLEZ}
      - OPENROUTER_TITLE=${OPENROUTER_TITLE:-OOGA}
      - REDIS_URL=${REDIS_URL}
      - PORT=8981
      - CELERY_TOTAL_CONCURRENCY=${CELERY_TOTAL_CONCURRENCY:-20}
      - INGEST_URL=${INGEST_URL:-https://datagen.stera.ventures/ingest}
    command: sh -c 'echo "Starting Celery worker with total concurrency ${CELERY_TOTAL_CONCURRENCY:-20}"; celery -A datagen.celery_app.celery_app worker -E --loglevel=INFO --concurrency="${CELERY_TOTAL_CONCURRENCY:-20}"'
    restart: unless-stopped

  celery-worker-2:
    build:
      context: ..
      dockerfile: datagen/Dockerfile
      args:
        PYTHON_IMAGE: ${PYTHON_IMAGE:-python:3.13-slim}
    image: nanochat-datagen:latest
    container_name: nanochat-datagen-worker-2
    depends_on:
      - datagen
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - OPENROUTER_MODELS=${OPENROUTER_MODELS:-}
      - OPENROUTER_REFERRER=${OPENROUTER_REFERRER:-BRADDLEZ}
      - OPENROUTER_TITLE=${OPENROUTER_TITLE:-OOGA}
      - REDIS_URL=${REDIS_URL}
      - PORT=8981
      - CELERY_TOTAL_CONCURRENCY=${CELERY_TOTAL_CONCURRENCY:-20}
      - INGEST_URL=${INGEST_URL:-https://datagen.stera.ventures/ingest}
    command: sh -c 'echo "Starting Celery worker with total concurrency ${CELERY_TOTAL_CONCURRENCY:-20}"; celery -A datagen.celery_app.celery_app worker -E --loglevel=INFO --concurrency="${CELERY_TOTAL_CONCURRENCY:-20}" --hostname="%h-w2"'
    restart: unless-stopped

  celery-worker-3:
    build:
      context: ..
      dockerfile: datagen/Dockerfile
      args:
        PYTHON_IMAGE: ${PYTHON_IMAGE:-python:3.13-slim}
    image: nanochat-datagen:latest
    container_name: nanochat-datagen-worker-3
    depends_on:
      - datagen
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - OPENROUTER_MODELS=${OPENROUTER_MODELS:-}
      - OPENROUTER_REFERRER=${OPENROUTER_REFERRER:-BRADDLEZ}
      - OPENROUTER_TITLE=${OPENROUTER_TITLE:-OOGA}
      - REDIS_URL=${REDIS_URL}
      - PORT=8981
      - CELERY_TOTAL_CONCURRENCY=${CELERY_TOTAL_CONCURRENCY:-20}
      - INGEST_URL=${INGEST_URL:-https://datagen.stera.ventures/ingest}
    command: sh -c 'echo "Starting Celery worker with total concurrency ${CELERY_TOTAL_CONCURRENCY:-20}"; celery -A datagen.celery_app.celery_app worker -E --loglevel=INFO --concurrency="${CELERY_TOTAL_CONCURRENCY:-20}" --hostname="%h-w3"'
    restart: unless-stopped

  celery-worker-4:
    build:
      context: ..
      dockerfile: datagen/Dockerfile
      args:
        PYTHON_IMAGE: ${PYTHON_IMAGE:-python:3.13-slim}
    image: nanochat-datagen:latest
    container_name: nanochat-datagen-worker-4
    depends_on:
      - datagen
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - OPENROUTER_MODELS=${OPENROUTER_MODELS:-}
      - OPENROUTER_REFERRER=${OPENROUTER_REFERRER:-BRADDLEZ}
      - OPENROUTER_TITLE=${OPENROUTER_TITLE:-OOGA}
      - REDIS_URL=${REDIS_URL}
      - PORT=8981
      - CELERY_TOTAL_CONCURRENCY=${CELERY_TOTAL_CONCURRENCY:-20}
      - INGEST_URL=${INGEST_URL:-https://datagen.stera.ventures/ingest}
    command: sh -c 'echo "Starting Celery worker with total concurrency ${CELERY_TOTAL_CONCURRENCY:-20}"; celery -A datagen.celery_app.celery_app worker -E --loglevel=INFO --concurrency="${CELERY_TOTAL_CONCURRENCY:-20}" --hostname="%h-w4"'
    restart: unless-stopped

  celery-worker-5:
    build:
      context: ..
      dockerfile: datagen/Dockerfile
      args:
        PYTHON_IMAGE: ${PYTHON_IMAGE:-python:3.13-slim}
    image: nanochat-datagen:latest
    container_name: nanochat-datagen-worker-5
    depends_on:
      - datagen
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - OPENROUTER_MODELS=${OPENROUTER_MODELS:-}
      - OPENROUTER_REFERRER=${OPENROUTER_REFERRER:-BRADDLEZ}
      - OPENROUTER_TITLE=${OPENROUTER_TITLE:-OOGA}
      - REDIS_URL=${REDIS_URL}
      - PORT=8981
      - CELERY_TOTAL_CONCURRENCY=${CELERY_TOTAL_CONCURRENCY:-20}
      - INGEST_URL=${INGEST_URL:-https://datagen.stera.ventures/ingest}
    command: sh -c 'echo "Starting Celery worker with total concurrency ${CELERY_TOTAL_CONCURRENCY:-20}"; celery -A datagen.celery_app.celery_app worker -E --loglevel=INFO --concurrency="${CELERY_TOTAL_CONCURRENCY:-20}" --hostname="%h-w5"'
    restart: unless-stopped

  celery-worker-6:
    build:
      context: ..
      dockerfile: datagen/Dockerfile
      args:
        PYTHON_IMAGE: ${PYTHON_IMAGE:-python:3.13-slim}
    image: nanochat-datagen:latest
    container_name: nanochat-datagen-worker-6
    depends_on:
      - datagen
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - OPENROUTER_MODELS=${OPENROUTER_MODELS:-}
      - OPENROUTER_REFERRER=${OPENROUTER_REFERRER:-BRADDLEZ}
      - OPENROUTER_TITLE=${OPENROUTER_TITLE:-OOGA}
      - REDIS_URL=${REDIS_URL}
      - PORT=8981
      - CELERY_TOTAL_CONCURRENCY=${CELERY_TOTAL_CONCURRENCY:-20}
      - INGEST_URL=${INGEST_URL:-https://datagen.stera.ventures/ingest}
    command: sh -c 'echo "Starting Celery worker with total concurrency ${CELERY_TOTAL_CONCURRENCY:-20}"; celery -A datagen.celery_app.celery_app worker -E --loglevel=INFO --concurrency="${CELERY_TOTAL_CONCURRENCY:-20}" --hostname="%h-w6"'
    restart: unless-stopped

volumes:
  datagen_data:
    driver: local


